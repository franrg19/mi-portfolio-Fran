// Configuración de Tailwind CSS
tailwind.config = {
    theme: {
        extend: {
            colors: {
                background: '#0f141e', // Color de fondo oscuro principal
                primary: '#1d8bf8',    // Azul brillante para botones y texto
                surface: '#1e293b',    // Color de las tarjetas/ventanas
            },
            fontFamily: {
                sans: ['Inter', 'system-ui', 'sans-serif'],
            }
        }
    }
}